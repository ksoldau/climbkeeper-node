type Query {
  info: String!
  feed: [Climb!]!
}

type Mutation {
  post(color: String!, gym: String!, tick: String!): Climb!
}

type Climb {
  id: ID!
  color: String!
  gym: String!
  tick: String!
}
